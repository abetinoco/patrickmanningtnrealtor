# IDX Broker Custom CSS

**Use `all-in-one.css`** – paste it into **IDX Dashboard → Design → Custom CSS → Global → Desktop**. One file, one paste, covers all page types.

For category-based setup (e.g. if you need page-specific overrides), see `docs/idx-css-by-idx/README.md`.
